<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calculadora Mercap</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/images/logo.ico">
    
    <link rel="stylesheet" href="/css/main.css">
    <style>
        .upload-area {
            border: 2px dashed var(--border-color);
            border-radius: 8px;
            padding: 24px;
            text-align: center;
            background: rgba(30, 142, 62, 0.05);
            cursor: pointer;
            transition: all 0.2s;
            margin-bottom: 24px;
        }
        .upload-area:hover {
            border-color: var(--primary-color);
            background: rgba(26, 115, 232, 0.05);
        }
        .upload-area.dragover {
            border-color: var(--primary-color);
            background: rgba(26, 115, 232, 0.1);
        }
        .partida-card {
            background: white;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 16px;
            box-shadow: var(--shadow);
            border-left: 4px solid var(--primary-color);
        }
        .partida-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
            padding-bottom: 12px;
            border-bottom: 1px solid var(--border-color);
            cursor: pointer;
            user-select: none;
            transition: background 0.2s;
        }
        .partida-header:hover {
            background: rgba(26, 115, 232, 0.05);
            border-radius: 4px;
            padding: 12px;
            margin: -12px -12px 4px -12px;
        }
        .partida-header::after {
            content: '';
            width: 0;
            height: 0;
            border-left: 6px solid transparent;
            border-right: 6px solid transparent;
            border-top: 8px solid var(--text-secondary);
            transition: transform 0.2s;
            margin-left: 12px;
        }
        .partida-card.collapsed .partida-header::after {
            transform: rotate(-90deg);
        }
        .partida-info {
            display: flex;
            gap: 24px;
            margin-bottom: 16px;
            align-items: flex-start;
        }
        .partida-info-fecha {
            flex-shrink: 0;
            min-width: 100px;
        }
        .partida-info-resto {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 12px;
            flex: 1;
        }
        .partida-info-item {
            display: flex;
            flex-direction: column;
            gap: 4px;
        }
        .partida-info-label {
            font-size: 12px;
            color: var(--text-secondary);
            font-weight: 500;
        }
        .partida-info-value {
            font-size: 14px;
            color: var(--text-primary);
            font-weight: 500;
        }
        .imputaciones-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 12px;
        }
        .imputaciones-table th {
            background: #f1f3f4;
            padding: 8px 12px;
            text-align: left;
            font-size: 12px;
            font-weight: 500;
            color: var(--text-secondary);
            border-bottom: 1px solid var(--border-color);
        }
        .imputaciones-table td {
            padding: 8px 12px;
            font-size: 13px;
            border-bottom: 1px solid #f1f3f4;
        }
        .imputaciones-table tr:hover {
            background: #f8f9fa;
        }
        .saldo-positivo {
            color: #1e8e3e;
            font-weight: 600;
        }
        .saldo-cero {
            color: var(--text-secondary);
        }
        .saldo-negativo {
            color: #d93025;
            font-weight: 600;
        }
        .error-card {
            background: #fce8e6;
            border-left: 4px solid #d93025;
            padding: 16px;
            margin-bottom: 16px;
            border-radius: 8px;
        }
        .error-title {
            color: #d93025;
            font-weight: 600;
            margin-bottom: 8px;
        }
        .error-message {
            color: #5f6368;
            font-size: 14px;
        }
        .loading {
            text-align: center;
            padding: 40px;
            color: var(--text-secondary);
        }
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
            margin-bottom: 24px;
        }
        .stat-card {
            background: white;
            padding: 16px;
            border-radius: 8px;
            box-shadow: var(--shadow);
        }
        .stat-label {
            font-size: 12px;
            color: var(--text-secondary);
            margin-bottom: 8px;
        }
        .stat-value {
            font-size: 24px;
            font-weight: 600;
            color: var(--primary-color);
        }
        .imputacion-cantidad-diferente {
            background: rgba(128, 128, 128, 0.1) !important;
        }
        .partida-card {
            position: relative;
        }
        .partida-content {
            transition: all 0.3s ease;
            overflow: hidden;
        }
        .partida-card.collapsed .partida-content {
            max-height: 0;
            opacity: 0;
            margin: 0;
            padding: 0;
        }
        .info-panel {
            font-size: 13px;
            line-height: 1.6;
        }
        .info-panel-compact {
            font-size: 11px;
            line-height: 1.5;
        }
        .info-section {
            margin-bottom: 20px;
        }
        .info-section-title {
            font-size: 14px;
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 12px;
            padding-bottom: 8px;
            border-bottom: 1px solid var(--border-color);
        }
        .info-item {
            margin-bottom: 16px;
            padding: 12px;
            background: rgba(26, 115, 232, 0.05);
            border-radius: 6px;
            border-left: 3px solid var(--primary-color);
        }
        .info-item-compact {
            margin-bottom: 6px;
            padding: 6px 10px;
            background: rgba(26, 115, 232, 0.05);
            border-radius: 4px;
            border-left: 2px solid var(--primary-color);
            font-size: 11px;
            line-height: 1.4;
        }
        .info-item strong {
            color: var(--text-primary);
            display: block;
            margin-bottom: 6px;
        }
        .info-item-compact strong {
            color: var(--text-primary);
            margin-right: 6px;
        }
        .info-item ul {
            margin: 6px 0 0 0;
            padding-left: 20px;
            color: var(--text-secondary);
        }
        .info-item li {
            margin-bottom: 4px;
        }
        .sql-query-container {
            position: relative;
        }
        .copy-sql-btn {
            position: absolute;
            top: 8px;
            right: 8px;
            display: flex;
            align-items: center;
            gap: 6px;
            padding: 6px 12px;
            background: var(--primary-color);
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 11px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s;
            font-family: 'Google Sans', 'Roboto', sans-serif;
        }
        .copy-sql-btn:hover {
            background: #1557b0;
            box-shadow: 0 1px 2px 0 rgba(60,64,67,.3), 0 1px 3px 1px rgba(60,64,67,.15);
        }
        .copy-sql-btn:active {
            transform: scale(0.98);
        }
        .copy-sql-btn svg {
            width: 14px;
            height: 14px;
        }
        .copy-sql-btn.copied {
            background: #1e8e3e;
        }
        .copy-sql-btn.copied:hover {
            background: #1a7e35;
        }
        @media (max-width: 1024px) {
            .upload-info-container {
                grid-template-columns: 1fr !important;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
    
    <div class="app-container">
        <main class="main-content">
            <div class="content-area">
                <h1 class="page-title" style="margin-bottom: 24px; color: #5f6368;">Análisis de inventario Unitrade</h1>
                
                <!-- Área de carga de archivo y panel informativo -->
                <div style="display: grid; grid-template-columns: 1fr 350px; gap: 20px; margin-bottom: 24px; align-items: start;" class="upload-info-container">
                    <!-- Área de carga de archivo -->
                    <div class="card" id="cardImportarExcel">
                        <h3 style="font-size: 18px; font-weight: 500; margin-bottom: 16px; color: #5f6368;">Importar Excel</h3>
                        <div class="upload-area" id="uploadArea" onclick="document.getElementById('fileInput').click()">
                            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="color: var(--primary-color); margin-bottom: 12px;">
                                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                                <polyline points="17 8 12 3 7 8"></polyline>
                                <line x1="12" y1="3" x2="12" y2="15"></line>
                            </svg>
                            <p style="font-size: 16px; color: var(--text-primary); margin-bottom: 8px;">
                                Haz clic para seleccionar un archivo Excel o arrastra y suelta aquí
                            </p>
                            <p style="font-size: 12px; color: var(--text-secondary);">
                                Columnas esperadas: TIPO_MIN, TIPO_MOV, MINUTA_ORIGEN, CANTIDAD, FECHA (DD/MM/AAAA)
                            </p>
                        </div>
                        <input type="file" id="fileInput" accept=".xlsx,.xls" style="display: none;" />
                        
                        <!-- Query SQL para generar el Excel -->
                        <div style="margin-top: 20px; padding-top: 20px; border-top: 1px solid var(--border-color);">
                            <p style="font-size: 12px; color: var(--text-secondary); margin-bottom: 8px;">
                                El Excel se puede generar ejecutando esta query en la base de datos:
                            </p>
                            <div style="position: relative;">
                                <pre id="sqlQuery" style="background: #f8f9fa; border: 1px solid var(--border-color); border-radius: 6px; padding: 10px; font-size: 11px; font-family: 'Courier New', monospace; overflow-x: auto; margin: 0; line-height: 1.4; color: var(--text-primary);"><code>SELECT minuta.TIPO AS TIPO_MIN, mov.TIPO AS TIPO_MOV, minuta.NUMERO as MINUTA_ORIGEN , CAST(mov.CANTIDAD AS BIGINT) AS CANTIDAD, CONVERT(VARCHAR(10), mov.FECHA, 23) AS FECHA
FROM MOVIMIENTOS_T AS mov INNER JOIN MINUTAS_T AS minuta ON mov.MINUTA_ORIGEN = minuta.OBJECT_ID 
WHERE mov.NRO_COMITENTE = '000002856' AND mov.ESPECIE = 'TTM26' AND mov.ESTADO = 'C' AND minuta.ANULADO IS NULL AND (mov.TIPO = 'I' OR mov.TIPO = 'E') 
ORDER BY mov.FECHA ASC</code></pre>
                                <button onclick="copiarQuerySQL()" class="copy-sql-btn" title="Copiar query">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                        <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                                        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
                                    </svg>
                                    <span>Copiar</span>
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Panel informativo -->
                    <div class="card" id="cardReglas" style="overflow-y: auto;">
                        <h3 style="font-size: 16px; font-weight: 500; margin-bottom: 12px; color: #5f6368;">Reglas de Ordenamiento</h3>
                        <div class="info-panel-compact">
                            <div class="info-item-compact">
                                <strong>A - Ingresos (Contados):</strong> INGR, C
                            </div>
                            <div class="info-item-compact">
                                <strong>B - Ingresos (patas en distintas fechas):</strong> PP, OCT, PRET/PRESTAMO (pata futura) | PA (contado), PFT (contado)
                            </div>
                            <div class="info-item-compact">
                                <strong>C - Ingresos por Transferencia:</strong> TRFU, TRFS, TRFC
                            </div>
                            <div class="info-item-compact">
                                <strong>D - Egresos (ambas patas misma fecha):</strong> PRET/PRESTAMO, PP, OCT (pata futuro)
                            </div>
                            <div class="info-item-compact">
                                <strong>E - Ingresos (ambas patas misma fecha):</strong> PRET/PRESTAMO, PP, OCT
                            </div>
                            <div class="info-item-compact">
                                <strong>F - Demás ingresos no clasificados</strong>
                            </div>
                            <div class="info-item-compact">
                                <strong>G - Egresos Futuros:</strong> PRET/PRESTAMO, PP, OCT
                            </div>
                            <div class="info-item-compact">
                                <strong>H - Partidas manuales:</strong> PARTIDA, PARTIDA_MANUAL, PM
                            </div>
                            <div class="info-item-compact">
                                <strong>I - Bloqueos/Garantías:</strong> BLOQUEO, GARANTIA, GARANTÍA, BLOQ, GAR
                            </div>
                            <div class="info-item-compact">
                                <strong>J - Egresos que impactan en PA PFT y OCT:</strong> PA, PFT, OCT
                            </div>
                            <div class="info-item-compact">
                                <strong>K - Egresos (Contados):</strong> V, TRFU, TRFC, TRFS, EGR
                            </div>
                            <div class="info-item-compact">
                                <strong>L - Demás egresos no clasificados</strong>
                            </div>
                            <div class="info-item-compact">
                                <strong>M - Imputaciones manuales:</strong> IMPUTACION, IMPUTACIÓN, IMPUT, IMPUT_MANUAL
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Estadísticas -->
                <div id="statsContainer" style="display: none;">
                    <div class="stats-container">
                        <div class="stat-card">
                            <div class="stat-label">Total Movimientos</div>
                            <div class="stat-value" id="statTotalMovimientos">0</div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-label">Total Partidas</div>
                            <div class="stat-value" id="statTotalPartidas">0</div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-label">Sumatoria de Saldos Partidas</div>
                            <div class="stat-value" id="statSumatoriaSaldos" style="color: var(--primary-color);">0.00</div>
                        </div>
                    </div>
                    <!-- Desglose de partidas con saldo -->
                    <div id="desgloseSaldosContainer" style="display: none; margin-top: 20px;"></div>
                </div>

                <!-- Errores -->
                <div id="erroresContainer" style="display: none;"></div>

                <!-- Partidas -->
                <div id="partidasContainer" style="display: none;"></div>

                <!-- Loading -->
                <div id="loadingContainer" class="loading" style="display: none;">
                    <p>Procesando archivo Excel...</p>
                </div>
            </div>
        </main>
    </div>

    <script src="/js/main.js"></script>
    <script src="/js/inventario.js"></script>
</body>
</html>


